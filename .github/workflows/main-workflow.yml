# ^   MAIN WORKFLOW

name: Main Flow
on:
  push:
    branch:
      - test**

jobs:
  # Calling reuable workflow
  sonarcloud_1:
    uses: gisaacsotelo/reusable-workflows/.github/workflows/new-flow.yml@dev
    with:
      path: "openloop/apf1"
      solution: "[ 'apf1', 'apf2', 'tf' ]"