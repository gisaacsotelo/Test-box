# ^   MAIN WORKFLOW

name: Main Flow (Node)
on:
  push:
    branches:
      - test/reusable**
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  # Calling reuable workflow
  sonarcloudScan_tf:
    name: Analyze Path
    uses: gisaacsotelo/reusable-workflows/.github/workflows/reusable-flow.yml@43734503c974fdd00b7d4b6d3676245dcbddee92
    with:
      path: "mycompany/myproject/mysolution"